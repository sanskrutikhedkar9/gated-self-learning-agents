# Either spelling is supported because the original prototype used the first.
ANTROPIC_API_KEY=
ANTHROPIC_API_KEY=
SELF_LEARNING_FLOWS_LLM=claude-sonnet-4-6

# Optional local SLM endpoint (Ollama, vLLM, llama.cpp server, etc.).
# Base URL or full /chat/completions URL; from_env() accepts either.
OPENAI_COMPATIBLE_BASE_URL=http://localhost:11434/v1
OPENAI_COMPATIBLE_API_KEY=local
OPENAI_COMPATIBLE_MODEL=qwen3:4b
